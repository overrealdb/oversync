apiVersion: v1
kind: Service
metadata:
  name: {{ include "oversync.fullname" . }}
  labels:
    {{- include "oversync.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "oversync.selectorLabels" . | nindent 4 }}
