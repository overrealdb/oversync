[package]
name = "oversync-delta"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Hash-based delta detection engine for oversync."

[dependencies]
oversync-core = { workspace = true }
